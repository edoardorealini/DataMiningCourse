def generate_association_rules(frequent_itemset):
    # Given one single itemset as input, the function generates all the possible association rules in form of tuples
    # (antecedent, consequence)

    return


def filter_association_rules(association_rules_list, confidence_threshold):
    # Main idea here, if I find one association rule that is below confidence I can skip all the others that have less literals in the antecedent.

    return


def compute_confidence(association_rule_tuple):
    # Compute the confidence level of the given association rule

    return